name: CI Workflow
on: [push]
jobs:
  build:
    name: Hello World
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        name: Checkout code
      - run: echo "Hello world"
        name: Echo greeting 
      - run: ls -la
        name: List files

  build2:
    name: Hello World
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        name: Checkout code
      - run: echo "Hello world"
        name: Echo greeting 
      - run: ls -la
        name: List files      
